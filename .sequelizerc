const path = require('path');

module.exports = {
  'config': path.resolve('src/api/config/config.js'), // Path to your config file
  'models-path': path.resolve('src/api/models'), // Path to your models directory
  'migrations-path': path.resolve('src/api/migrations'), // Path to your migrations directory
  'seeders-path': path.resolve('src/api/seeders') // Path to your seeders directory
};
